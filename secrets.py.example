# This file is where you keep secret settings, passwords, and tokens!
# If you put them in the code you risk committing that info or sharing it
# which would be not great. So, instead, keep it all in this one file and
# keep it a secret.

secrets = {
    'ssid' : 'your-wifi-ssid',           # Keep the two '' quotes around the name
    'password' : 'your-wifi-password',    # Keep the two '' quotes around password
    'timezone' : "America/Denver",        # http://worldtimeapi.org/timezones
    'aio_username' : 'your-adafruit-io-username',
    'aio_key' : 'your-adafruit-io-key',
    'openweather_token' : 'your-openweathermap-api-key'
    }
